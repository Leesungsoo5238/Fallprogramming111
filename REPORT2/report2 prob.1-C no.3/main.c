#include <stdio.h>

int main(void)
{
  int kor = 70, eng = 80, mat = 90;
  int tot;

  tot = kor + eng + mat;
  printf("kor : %d, eng : %d, mat : %d/n", kor, eng, mat);
  printf("tot : %d", tot);

  return 0;
}